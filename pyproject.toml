[project]
name = "pm25-prediction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matheus Mello", email = "matheusmellocunha@gmail.com" }
]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
pm25-prediction = "pm25_prediction:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
